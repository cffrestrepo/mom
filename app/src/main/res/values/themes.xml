<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Meli" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/yellow_200</item>}
        <item name="colorPrimaryDark">@color/black</item>
        <item name="actionMenuTextColor">@color/black</item>
        <item name="colorPrimaryVariant">@color/yellow_400</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <item name="colorAccent">@color/black</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Meli.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.Meli.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light" />
    <style name="Theme.Meli.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>